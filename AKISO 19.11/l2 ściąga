#zad1
#(5pt) Wytłumacz jakie pliki zawierają katalogi /dev oraz /proc. 

/dev - pliki urządzeń
/proc - informacje o procesach, procesorze, dyskach itp. Illusionary

#odczytaj pierwszy sektor z dysku głównego (uwaga na prawa dostępu) lub podpiętego pendrive'a i wyświetl przez hexdump -C. 

sudo dd if=/dev/sda3 bs=512 skip=900 count=1 | hexdump -C

#Z katalogu proc wyświetl informacje o pamięci oraz procesorze.

cat /proc/cpuinfo
cat /proc/meminfo

#zad2
PID procesu i rodzica, stan procesu, priorytet, nice, ilość pamięci, zużycie czasu procesora

ps -eo user,pid,ppid,stat,pri,nice,%mem,%cpu

#Uruchom też kilka terminali pokaż jakie urządzenie tty wykorzystują. Wykonując komendę ps axjf pokaż wszystkie procesy które podpięte są do #tych terminali (kolumna TTY).
a)
tty (cd /dev/pts)
b)
ps axjf | grep pts

#zad4
#Uruchom potok co najmniej trzech poleceń działających przez jakiś zauważalny czas. Mogą to być odpowiednio dobrane polecenia systemowe. Sprawdź #poleceniem ps i odpowiedz jakie zachodzi pokrewieństwo między tymi procesami (jeśli w ogóle zachodzi)

sleep 100 | sleep 100 | sleep 100 &
ps -ely | grep sleep
ppid takie samo

#zad 5
terminal1: mfkfifo fif1 ; cat > fif1
terminal2: cat < fif1





